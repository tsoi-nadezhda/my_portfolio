{
  "name": "portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Portfolio site with React client and Express mail API",
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client"
  }
}
